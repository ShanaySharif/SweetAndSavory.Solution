
 @{
    Layout = "_Layout";
}

@using SweetAndSavory.Models;

<div class="container">
    <p>@Html.ActionLink("Create or manage an account", "Index", "Account")</p>
    <h1 class="text-center">Welcome to Piers Sweet and Savory Treats</h1>
</div>
<hr />
<div class="text-center container-wrapper">
    <div class="section">
        <h2 class="mb-3">Treats</h2>
        <p>
            @Html.ActionLink("Manage Treats", "Index", "Treats", new { @class = "btn btn-primary" })
        </p>

     @if (Model["treats"] == null || Model["treats"].Length == 0) 

        {
            <p>No treats have been added yet!</p> 
        } 
        else
        {
            <ul class="list-group">
                @foreach (Treat treat in Model["treats"])
                {
                    <li class="list-group-item">
                         @Html.ActionLink(@treat.TreatName, "Details", "Treats", new { id = @treat.TreatId }, new { @class = "link-item" }) 
                    </li> 
                }
            </ul>
        }
    </div>

    <div class="section">
        <h2 class="mb-3">Flavors</h2>
        <p>
             @Html.ActionLink("Manage Flavors", "Index", "Flavors", new { @class = "btn btn-primary" }) 
        </p>

        @if (Model["flavors"] == null || Model["flavors"].Length == 0)
        {
            <p>No Flavors have been added yet!</p>
        }
        else
        {
            <ul class="list-group">
                @foreach (Flavor flavor in Model["flavors"])
                {
                    <li class="list-group-item">
                        @Html.ActionLink(@flavor.FlavorName, "Details", "Flavors", new { id = @flavor.FlavorId }, new { @class = "link-item" })
                    </li>
                }
            </ul>
        }
    </div>
</div>
<hr /> 


  @* @{
  Layout = "_Layout";
}
<body> 

<div class="container"> 

<h1>Welcome !!!</h1>

</div>

<div class="container">
@Html.ActionLink("See all FLavors", "Index", "Flavors", null, new { @class = "custom-button" })

@Html.ActionLink("See all Treats", "Index", "Treats", null, new { @class = "custom-button" })


</div>

</body> *@

@* 
@{
  Layout = "_Layout";
}




/// Comment out code below. it's giving SQL ex
@using SweetAndSavory.Models;

<h1>Welcome to Piers Sweet and Savory Treats!</h1>

<hr>

<h4>Flavors</h4>
@if (Model["flavors"].Length == 0)
{
  <p>No Flavs Availabe at this time!!</p>
} 
<ul>
  @foreach (Flavor flavor in Model["flavors"])
  {
    <li>@Html.ActionLink(@flavor.FlavorName, "Details", "Flavors", new { id = @flavor.FlavorId})</li>
  }
</ul>

<h4>Treats</h4>
@if (Model["treats"].Length == 0)
{
  <p>No Treats availabe at this time!!</p>
} 
<ul>
  @foreach (Treat treat in Model["treats"])
  {
    <li>@Html.ActionLink(@treat.TreatName, "Details", "Treats", new { id = @treat.TreatId})</li>
  }
</ul>

<hr>
@if (User.Identity.IsAuthenticated)
{
  <p>@Html.ActionLink("Add a new flavor", "Create", "Flavors")</p>
  <p>@Html.ActionLink("Add new treat", "Create", "Treats")</p>
  <p>@Html.ActionLink("View flavors", "Index", "Flavors")</p>
  <p>@Html.ActionLink("View treats", "Index", "Treats")</p>
}
<p>Account<br>@Html.ActionLink("Register / Login", "Index", "Account")</p> *@